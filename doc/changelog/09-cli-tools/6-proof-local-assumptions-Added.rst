- **Added:**
  ``rocqchk`` options ``--proof-assumptions`` and
  ``--proof-assumptions-output`` produce deterministic, versioned JSON
  attestations of the exact transitive constant assumptions reachable from
  selected checked declarations
  (`#6 <https://github.com/theorem-labs/rocq/pull/6>`_,
  by Archana Burra).
